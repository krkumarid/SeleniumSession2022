package SeleniumSessions;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.remote.RemoteWebDriver;
import org.openqa.selenium.safari.SafariDriver;

public class WebDriverCastings
{

    public static void main(String[] args)
    {
        /*
         * // 3.RWD -CD // thsis is happening in local only RemoteWebDriver driver = new
         * ChromeDriver(); driver = new FirefoxDriver(); driver = new SafariDriver();
         * //4 WebDriver -RemoteWebDriver
         * 
         * // WebDriver dr = new RemoteWebDriver()
         */    }

}
